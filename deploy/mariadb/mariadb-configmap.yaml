apiVersion: v1
kind: ConfigMap
metadata:
  name: mariadb-config
  namespace: REPLACE_NAMESPACE
  labels:
    app: mariadb
data:
  MYSQL_DATABASE: installer
  MYSQL_USER: admin
  MYSQL_PASSWORD: admin
  MYSQL_ROOT_PASSWORD: root